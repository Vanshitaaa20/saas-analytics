"use client";
import {useState} from "react"; import Link from "next/link"; import {useRouter} from "next/navigation"; import {api} from "@/lib/api"; import {Sparkles,ArrowRight} from "lucide-react";
export default function LoginPage(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const router=useRouter();const handleSubmit=async(e:React.FormEvent)=>{e.preventDefault();setError("");try{const r=await api.post("/auth/login",{email,password});localStorage.setItem("accessToken",r.data.accessToken);router.push("/dashboard")}catch{setError("Invalid email or password")}};return <main className="app-shell flex min-h-screen items-center justify-center p-5"><div className="noise"/><section className="glass relative z-10 grid w-full max-w-5xl overflow-hidden rounded-[28px] md:grid-cols-[1.05fr_.95fr]"><div className="hidden min-h-[540px] flex-col justify-between bg-gradient-to-br from-blue-500/25 via-violet-500/15 to-transparent p-10 md:flex"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl glow-button"><Sparkles size={19}/></span><span className="text-xl font-semibold">Pulse<span className="text-blue-400">lytics</span></span></div><div><p className="text-4xl font-semibold leading-tight">Turn product signals into your next move.</p><p className="mt-5 max-w-sm text-sm leading-6 text-[#a3b1ce]">A beautifully clear view of your SaaS performance, built for every team.</p></div><p className="text-xs text-[#8291b2]">Multi-tenant analytics, without the noise.</p></div><form onSubmit={handleSubmit} className="bg-[#08102e]/55 p-7 sm:p-10"><div className="mb-10 md:hidden flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl glow-button"><Sparkles size={17}/></span><span className="text-lg font-semibold">Pulselytics</span></div><p className="text-xs font-semibold uppercase tracking-[.2em] text-blue-300">Welcome back</p><h1 className="mt-3 text-3xl font-semibold text-white">Sign in to your workspace</h1><p className="mt-2 text-sm text-[#91a0c3]">Enter your details to continue.</p>{error&&<p className="mt-5 rounded-xl border border-red-400/20 bg-red-400/10 p-3 text-sm text-red-200">{error}</p>}<label className="mt-8 block text-xs font-medium text-[#b6c3df]">Email<input required type="email" placeholder="you@company.com" value={email} onChange={e=>setEmail(e.target.value)} className="field mt-2"/></label><label className="mt-5 block text-xs font-medium text-[#b6c3df]">Password<input required type="password" placeholder="••••••••" value={password} onChange={e=>setPassword(e.target.value)} className="field mt-2"/></label><button className="glow-button mt-7 flex w-full items-center justify-center gap-2 rounded-xl py-3 text-sm font-medium text-white">Continue <ArrowRight size={16}/></button><p className="mt-6 text-center text-sm text-[#6f7b98]">New to Pulselytics? <Link href="/register" className="font-semibold text-blue-600 hover:text-violet-600">Create an account</Link></p></form></section></main>}
