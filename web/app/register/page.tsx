"use client";
import {useState} from "react"; import Link from "next/link"; import {useRouter} from "next/navigation"; import {api} from "@/lib/api"; import {Sparkles,ArrowRight,CheckCircle2} from "lucide-react";
export default function RegisterPage(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [submitting,setSubmitting]=useState(false);const router=useRouter();const submit=async(e:React.FormEvent)=>{e.preventDefault();setError("");setSubmitting(true);try{const r=await api.post("/auth/register",{email,password});localStorage.setItem("accessToken",r.data.accessToken);router.push("/dashboard")}catch(err:any){const message=err?.response?.data?.message;setError(Array.isArray(message)?message[0]:message??"We couldn't create your account. Please try again.")}finally{setSubmitting(false)}};return <main className="app-shell flex min-h-screen items-center justify-center p-5"><div className="noise"/><section className="glass relative z-10 grid w-full max-w-5xl overflow-hidden rounded-[28px] md:grid-cols-[1.05fr_.95fr]"><div className="hidden min-h-[560px] flex-col justify-between bg-gradient-to-br from-blue-500/25 via-violet-500/15 to-transparent p-10 md:flex"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl glow-button"><Sparkles size={19}/></span><span className="text-xl font-semibold">Pulse<span className="text-blue-400">lytics</span></span></div><div><p className="text-4xl font-semibold leading-tight">Your product’s story starts here.</p><div className="mt-7 space-y-3 text-sm text-[#667493]"><p className="flex items-center gap-2"><CheckCircle2 size={16} className="text-blue-500"/>One workspace for every team</p><p className="flex items-center gap-2"><CheckCircle2 size={16} className="text-blue-500"/>Live product intelligence</p><p className="flex items-center gap-2"><CheckCircle2 size={16} className="text-blue-500"/>Beautifully simple analytics</p></div></div><p className="text-xs text-[#8291b2]">Set up in less than a minute.</p></div><form onSubmit={submit} className="bg-[#08102e]/55 p-7 sm:p-10"><div className="mb-8 md:hidden flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl glow-button"><Sparkles size={17}/></span><span className="text-lg font-semibold">Pulselytics</span></div><p className="text-xs font-semibold uppercase tracking-[.2em] text-blue-600">Get started</p><h1 className="mt-3 text-3xl font-semibold text-white">Create your account</h1><p className="mt-2 text-sm text-[#6f7b98]">Start exploring your product analytics today.</p>{error&&<p className="mt-5 rounded-xl border border-red-400/20 bg-red-400/10 p-3 text-sm text-red-600">{error}</p>}<label className="mt-8 block text-xs font-medium text-[#52607d]">Work email<input required type="email" placeholder="you@company.com" value={email} onChange={e=>setEmail(e.target.value)} className="field mt-2"/></label><label className="mt-5 block text-xs font-medium text-[#52607d]">Password<input required minLength={8} type="password" placeholder="At least 8 characters" value={password} onChange={e=>setPassword(e.target.value)} className="field mt-2"/></label><button disabled={submitting} className="glow-button mt-7 flex w-full items-center justify-center gap-2 rounded-xl py-3 text-sm font-medium text-white disabled:cursor-not-allowed disabled:opacity-65">{submitting?"Creating your account...":"Create account"} <ArrowRight size={16}/></button><p className="mt-6 text-center text-sm text-[#6f7b98]">Already have an account? <Link href="/login" className="font-semibold text-blue-600 hover:text-violet-600">Sign in</Link></p></form></section></main>}
